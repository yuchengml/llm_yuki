# fixtures

Shared sample Raw Sources (folder + `.txt` + `images/`, per D10) and sample bundles go here as they're needed
by integration/e2e tests. Empty for now — current tests build their inputs inline via `tmp_path`.
