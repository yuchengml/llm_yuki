# e2e tests

Full-pipeline tests (`Connector` → `Orchestrator` → `Writer`, driven by real `Extractor`/`Validator`/`Fixer`
implementations) belong here. Empty until those LLM-backed components exist — see root `README.md`,
"POC Status".
