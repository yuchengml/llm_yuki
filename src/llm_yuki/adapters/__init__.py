"""Concrete I/O implementations of the ``llm_yuki.ports`` interfaces.

All filesystem/network access lives here. ``domain`` code must never import from this package — see root
ARCHITECTURE.md §3.
"""
